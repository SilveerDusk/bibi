input-file-directory — attacker-info turnaround targeting team 102 (via)

Origin: attacker-info/892-team108_input_file_is_directory (submission 892, original result: Break invalid).
This clone keeps the original command sequence and updates only target_team.

Target team: 108
Bug title: write accepts a directory as -i input
Bug type: correctness
Severity: 25 points

Command used:
  Register alice, create notes, then write with -i /tmp (a directory).

Observed behavior:
  The write command succeeds with exit 0 instead of rejecting the invalid input.

Expected behavior:
  A directory is not a valid input file. stor should print invalid and exit 255.

Why this violates the spec or security goal:
  Malformed -i paths must be rejected cleanly. Accepting a directory differs from
  the reference oracle and may lead to undefined reads.

Suspected file and line:
  stor108.c write handler: fopen(infile) on a directory succeeds without checking
  file type.

Minimal reproducer:
  stor -u alice -k secret123 register
  stor -u alice -f notes create
  stor -u alice -k secret123 -f notes -i /tmp write
