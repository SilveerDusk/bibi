input-file-device — attacker-info turnaround targeting team 105 (wizards)

Origin: attacker-info/890-team108_input_file_device (submission 890, original result: Break invalid).
This clone keeps the original command sequence and updates only target_team.

Target team: 108
Bug title: write accepts a device file as -i input
Bug type: correctness
Severity: 25 points

Command used:
  Register alice, create notes, then write with -i /dev/zero.

Observed behavior:
  The write command succeeds with exit 0 instead of rejecting the device path.

Expected behavior:
  Device files are not valid -i inputs. stor should print invalid and exit 255.

Why this violates the spec or security goal:
  Accepting special device paths differs from the reference oracle and can cause
  hangs or unbounded reads on other device nodes.

Suspected file and line:
  stor108.c write handler: no validation that -i names a regular file.

Minimal reproducer:
  stor -u alice -k k register
  stor -u alice -f notes create
  stor -u alice -k k -f notes -i /dev/zero write
