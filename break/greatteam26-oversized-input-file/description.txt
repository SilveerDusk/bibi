oversized-input-file — attacker-info turnaround targeting team 103 (greatteam26)

Origin: attacker-info/894-team108_oversized_input_file (submission 894, original result: Break invalid).
This clone keeps the original command sequence and updates only target_team.

Target team: 108
Bug title: write accepts a 300 MB -i input without rejection
Bug type: correctness
Severity: 25 points

Command used:
  Register alice, create file f, then write with -i pointing to a 300 MB sparse
  file.

Observed behavior:
  The write command exits 0 instead of rejecting the oversized input.

Expected behavior:
  Inputs beyond reasonable limits must be rejected gracefully with invalid/255.
  The reference oracle rejects this 300 MB file.

Why this violates the spec or security goal:
  Accepting unbounded input differs from the reference behavior and risks memory
  exhaustion or denial of service.

Suspected file and line:
  stor108.c write handler: no maximum content size check after ftell on -i file.

Minimal reproducer:
  truncate -s 300M /tmp/big.bin
  stor -u alice -k secret123 -f f -i /tmp/big.bin write
