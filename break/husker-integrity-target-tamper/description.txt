integrity-target-tamper — attacker-info turnaround targeting team 115 (husker)

Origin: attacker-info/1031-team108_integrity_target_tamper_undetected (submission 1031, original result: Break invalid).
This clone keeps the original command sequence and updates only target_team.

Target team: 108
Bug title: Mid-file byte tamper on integrity_target record is not detected
Bug type: integrity
Source testcase: security_focus_tests.sh K4_integrity_target_tamper_invalid

This is the organizer-style integrity scenario: after writing the standard
"integrity_target" content, flipping a byte in the middle of enc.db must cause
read to fail when the correct key is supplied. stor108 still returns the
original plaintext.

Minimal reproducer:
  stor -u user1 -k secret123 register
  stor -u user1 -f file1 create
  stor -u user1 -k secret123 -f file1 write integrity_target
  python3 -c "d=bytearray(open('enc.db','rb').read()); d[len(d)//2]^=0xFF; open('enc.db','wb').write(d)"
  stor -u user1 -k secret123 -f file1 read

Observed: stdout=integrity_target, exit 0
Expected: stdout=invalid, exit 255
