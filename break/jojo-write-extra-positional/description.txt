write-extra-positional — attacker-info turnaround targeting team 109 (jojo)

Origin: attacker-info/899-team108_write_extra_positional (submission 899, original result: Break invalid).
This clone keeps the original command sequence and updates only target_team.

Target team: 108
Bug title: write accepts two inline text arguments
Bug type: correctness
Severity: 25 points

Command used:
  Register alice, create notes, then write with two positional text args "one"
  and "two".

Observed behavior:
  write succeeds with exit 0 instead of rejecting the malformed invocation.

Expected behavior:
  write accepts at most one inline text argument. Extra positional tokens must
  produce invalid/255.

Why this violates the spec or security goal:
  Contradictory argument combinations must be rejected per spec.

Suspected file and line:
  stor108.c write handler: does not validate that optind+2 >= argc.

Minimal reproducer:
  stor -u alice -k secret123 -f notes write one two
