large-binary-roundtrip — attacker-info turnaround targeting team 101 (GT)

Origin: attacker-info/1115-large_binary_file_roundtrip (submission 1115, original result: Break invalid).
This clone keeps the original command sequence and updates only target_team.

Test case: large_binary_file_roundtrip
Type: correctness
Target team: 108

What it checks: large binary file under 600 MB preserved exactly.
How it triggers: generate fuzz590.txt, write via -i, read via -o, cmp.
Expected: all commands exit 0, cmp prints MATCH.
Counts as a break if: large file corrupted/truncated/rejected.
