Target team: 108
Bug title: read accepts an extra positional argument
Bug type: correctness
Severity: 25 points

Command used:
  Register, create, write hello, then read notes with a trailing "extra" argument.

Observed behavior:
  read prints the file content (hello) and exits 0 instead of rejecting the
  malformed invocation.

Expected behavior:
  read takes no inline text. An extra positional argument is invalid input and
  must produce invalid/255.

Why this violates the spec or security goal:
  Malformed command lines must be rejected. Ignoring extra arguments differs from
  the reference oracle.

Suspected file and line:
  stor108.c read handler: does not validate optind+1 < argc.

Minimal reproducer:
  stor -u alice -k secret123 -f notes read extra
